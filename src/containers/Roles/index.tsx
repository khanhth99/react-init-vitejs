import Header from '@components/Header';
import Page from '@components/Page';

function Roles() {
  return (
    <Page title="Roles">
      <Header title={['Setting', 'Roles']} />
      <p>đây là Roles</p>
      <p>đây là Roles</p>
    </Page>
  );
}

export default Roles;
